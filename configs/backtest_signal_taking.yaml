run_name: weather_signal_taking_main_v1
dataset: weather_nyc_main_v1_features

strategy:
  name: threshold_signal_taker
  min_edge_cents: 5
  quantity: 1

fees:
  mode: fixed_per_contract
  fixed_fee_cents_per_contract: 0.1
  include_fees: true
